{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=familyshop;Username=postgres;Password=CHANGE_ME"
  },
  "Jwt": {
    "SecretKey": "CHANGE_ME_replace_with_a_generated_secret_min_32_chars",
    "Issuer": "FamilyShop.Api",
    "Audience": "FamilyShop.Client",
    "AccessTokenExpirationMinutes": 15,
    "RefreshTokenExpirationDays": 14
  },
  "Cors": {
    "_todo": "https://familyshop.example is still a placeholder for the real frontend domain - replace before deploy. familyshop-production.up.railway.app (the backend's own Railway domain) was added alongside it, though CORS normally only needs to whitelist the *frontend's* origin (the page making cross-origin requests), not the API's own host - keep it only if something (Swagger UI, a health check page, etc.) actually calls this API from that origin, otherwise it's a harmless no-op entry.",
    "AllowedOrigins": ["https://familyshop.example", "https://familyshop-production.up.railway.app"]
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
