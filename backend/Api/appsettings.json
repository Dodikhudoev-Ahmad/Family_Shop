{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=familyshop;Username=postgres;Password=CHANGE_ME"
  },
  "Jwt": {
    "SecretKey": "CHANGE_ME_replace_with_a_generated_secret_min_32_chars",
    "Issuer": "FamilyShop.Api",
    "Audience": "FamilyShop.Client",
    "AccessTokenExpirationMinutes": 15,
    "RefreshTokenExpirationDays": 14
  },
  "Cors": {
    "_todo": "endearing-consideration-production-6e0c.up.railway.app is the real frontend domain - the entry CORS actually needs. https://familyshop.example is a now-stale placeholder from before that domain existed; remove it once the real domain is confirmed working. familyshop-production.up.railway.app (the backend's own Railway domain) was added on request, though CORS normally only needs to whitelist the *frontend's* origin, not the API's own host - keep it only if something (Swagger UI, a health check page, etc.) actually calls this API from that origin.",
    "AllowedOrigins": [
      "https://familyshop.example",
      "https://familyshop-production.up.railway.app",
      "https://endearing-consideration-production-6e0c.up.railway.app"
    ]
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
